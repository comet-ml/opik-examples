[project]
name = "opik-examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opik>=1.11.0",
]

[tool.uv.workspace]
members = [
    "guides/tracing_finetuned_models",
]
